namespace AlienGrab
{
    public enum CollisionType { None, Building, Roof };
    public enum ApplicationState { Splash, Home, Options, Quit, Trial, Playing, Paused, LevelComplete, GameOver, GameComplete };
}
