namespace AlienGrab
{
    public enum CollisionType { None, Building, Roof };
    public enum ApplicationState { Splash, InitaliseApp, Home, Options, Quit, Trial, InitaliseGame, Playing, Paused, LevelComplete, GameOver, GameComplete };
    public enum GamePadStateValues { Null, Disconnected, Connected };
}
